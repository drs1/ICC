class Fibonacci {
     void main(string[] args) {
	 int n,r;

	 n = 10;
	 r = fib(n);
        
	 Library.printi(r);
	 Library.println("");
     }

     int fib(int n) {
        if (n < 2) return n;
        return fib(n-1) + fib(n-2);
     } 
}
