//Output: Success.

class Tweetie{
       int bird;
}

class Salvestor{
       Tweetie birdieBoy;
       int main(){

       	   //expect this to break once we start checking if statment return types correctly
       	    birdieBoy = new Tweetie();
	    if(birdieBoy.bird + birdieBoy.bird > 7)
	       return birdieBoy.bird;
       }

}